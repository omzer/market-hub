import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary colors
  static const Color primaryGreen = Color(0xFF558B2F);

  // Accent colors
  static const Color accentOrange = Color(0xFFFFA726);
  static const Color accentOrangeDark = Color(0xFFFB8C00);

  // Neutral colors
  static const Color neutralBrown = Color(0xFF6D4C41);

  // Background colors
  static const Color backgroundBeige = Color(0xFFF8F8F0);
  static const Color backgroundGrey = Color(0xFFF0F2F5);

  // Text colors
  static const Color textPrimary = Color(0xFF333333);

  // Other colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color borderLight = Color(0xFFE0E0E0);
}
